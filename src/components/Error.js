import React from 'react';

const Error = () => {
    return(
        <div>
            <h1>Sorry. We could not the find the page that you were looking for.</h1>
        </div>
    );
};

export default Error;
